#version 330 core
out vec4 FragColor;

in vec2 TexCoords;

uniform float Time;
uniform sampler2D texTure;
uniform sampler2D textureColor;

void main()
{
    vec3 color = texture(textureColor, TexCoords).rgb;
    float sd = texture(texTure, TexCoords).r;
    //sd = max(min(1.0, sd+0.5), 0.0);
    FragColor =  vec4(sd, sd, sd, 1.0f);
}

